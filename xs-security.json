{
  "xsappname": "pdf-compression-service",
  "tenant-mode": "dedicated",
  "description": "XSUAA instance securing the PDF compression service for machine-to-machine calls (SAP CPI OAuth2ClientCredentials). No custom scopes: a client_credentials token only ever carries the default uaa.resource scope, so authorization here is simply 'the caller holds this instance's client secret' — see src/middleware/auth.js.",
  "oauth2-configuration": {
    "credential-types": ["binding-secret"]
  }
}
